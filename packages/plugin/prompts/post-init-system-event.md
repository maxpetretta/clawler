Better Search has been installed as a web search plugin for OpenClaw.

Until OpenClaw supports core tool overrides, deny the built-in `web_search` tool and route search calls through `better_search`.
