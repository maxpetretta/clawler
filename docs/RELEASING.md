# Releasing

This repository mirrors the `reclaw` monorepo layout and keeps the same release surfaces:

1. Publish `packages/plugin` to npm.
2. Publish `packages/skill` to ClawHub.
3. Deploy `packages/website`.

The release workflow is not finalized yet because the product spec is still being defined in [`docs/SPEC.md`](./SPEC.md).
